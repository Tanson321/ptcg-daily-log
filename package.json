{
  "name": "ptcg-daily-log",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "channels": "node scripts/list-channels.js",
    "fetch:today": "node scripts/fetch-today.js --range=today",
    "fetch:week": "node scripts/fetch-today.js --range=week",
    "generate": "npm run generate:today",
    "generate:today": "node scripts/generate-post.js --range=today",
    "generate:week": "node scripts/generate-post.js --range=week",
    "build:today": "npm run fetch:today && npm run generate:today",
    "build:week": "npm run fetch:week && npm run generate:week",
    "site": "node scripts/build-site.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/genai": "^2.6.0",
    "discord.js": "^14.26.4",
    "dotenv": "^17.4.2"
  }
}

{
  "name": "ptcg-daily-log",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "channels": "node scripts/list-channels.js",
    "fetch:today": "node scripts/fetch-today.js --range=today",
    "fetch:week": "node scripts/fetch-today.js --range=week",
    "generate": "node scripts/generate-post.js",
    "build:today": "npm run fetch:today && npm run generate",
    "build:week": "npm run fetch:week && npm run generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/genai": "^2.6.0",
    "discord.js": "^14.26.4",
    "dotenv": "^17.4.2"
  }
}
